name: nightly

on:
  workflow_dispatch: # 有局复未定义是大学

dobs:
  smoke:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps: 
      - name: Say hello
        run: echo "Nightly workflow is alive. $(date -u)" && exit 0