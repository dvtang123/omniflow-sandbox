name: hello
on:
  workflow_dispatch:
  push:
    paths:
      - ".githubworkflows/hello.yml"
jjobs:
  say:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello from GPTS" && exit 0
