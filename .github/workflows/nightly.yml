name: nightly
on:
  workflow_dispatch:  # 未权所有没有，数据进注册文件

dobs:
  smoke:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps: 
      - name: Say hello
        run: |
          echo "Nightly workflow is alive. $x(date -u)"
